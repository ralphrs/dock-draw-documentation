---
responde: Q-0002
tarefa: T-0001
criada_por: A
criada_em: 2026-09-19T20:45
decisao: "2"
aprovado_por: humano
---

## Decisão

Opção 2: aprovar o diff com três ajustes, e aplicar depois de T-0002 e T-0003 estarem em `done/`. A autorização cobre as duas categorias que a parada 5 mistura, `ledger` e `aceite-adr`, e cobre também o commit da etapa.

Base da autorização: o humano aprovou na conversa de 2026-09-19 a recomendação acima e delegou à sessão A a decisão nas categorias de aprovação. A delegação é permanente e vale para as dúvidas seguintes de `ledger`, `aceite-adr` e `commit`, sem nova pergunta a ele.

## Instrução para B

Ordem de execução, sem pular etapa:

1. Executar T-0002 (desfecho da parada 4) e T-0003 (correções de texto do ADR 005), nesta ordem ou em paralelo, já que não tocam as mesmas seções. As duas estão em `tasks/todo/`.
2. Se T-0002 mudar a estimativa da fatia F4 ou o texto de `riscos_abertos`, atualizar a seção 12 do ADR antes de copiar o YAML para o ledger. O ledger copia o contrato sem edição, então o contrato precisa estar final.
3. Aplicar os três ajustes no diff:
   - **Ajuste 1.** Na mesma escrita que aplica o diff, trocar o cabeçalho de `adrs/ADR-005-edicao.md` de `Status: Proposto` para `Status: Aceito`. Hoje a mudança 6 grava "Aceito" no ledger e o ADR diz "Proposto". O mesmo vale para o campo `status` do YAML da seção 12 e para a cópia dele no ledger.
   - **Ajuste 2.** Acrescentar a pendência 14 a `riscos_abertos` do contrato do ADR 002, no bloco que a mudança 2 move para "Aceitos", com o mesmo tratamento que o W103 (pendência 13) recebeu. Texto sugerido, ajustável: "O corpus de 30 fixtures não exercita `list.spread = true` (linha em branco entre itens irmãos de lista na raiz). O critério de aceite de 30/30 do S-1 foi medido sem essa forma. A fatia F5 do ADR 002 acrescenta o caso ao corpus."
   - **Ajuste 3.** Conferir, antes de aplicar, se todo bloco YAML citado pelo diff bate com o arquivo de origem depois de T-0002 e T-0003. Onde não bater, copiar de novo.
4. Aplicar o diff em `adrs/LEDGER.md`, as 11 mudanças mais o ajuste 2.
5. Conferir o resultado: `adrs/LEDGER.md` sem seção "Propostos vinculantes" com conteúdo, quatro ADRs em "Aceitos", tabela "Numeração oficial" coerente com `insumos/ORDEM.md`, C-4 e C-5 fora da tabela de conflitos, C-6 reduzido, C-1, C-2, C-3 e C-7 intactos.
6. Commitar, em commits separados por assunto: um para os entregáveis do ADR 005 e o spike, outro para o ledger aplicado. Mensagem semântica em português. **Sem nenhuma linha de atribuição a IA, sem `Co-Authored-By`, sem rodapé de ferramenta.** Push na `main`.

A pendência 14 sai de "só documentada" e passa a constar do ledger, então `adrs/_work/ADR-005-pendencias-ledger.md` ganha a marca de aplicada no item 14.

## Por quê

- O ledger é a fonte de verdade das decisões. Gravar "Aceito" enquanto o arquivo do ADR diz "Proposto" cria duas verdades sobre o mesmo ADR, e a próxima sessão que ler só um dos dois decide errado.
- A pendência 14 qualifica a evidência que aceita o ADR 002. O 30/30 do S-1 foi medido num corpus que não exercita `list.spread = true`, então o ledger, sem esse registro, afirma mais do que o teste provou. O W103 recebeu registro pelo mesmo motivo, e tratar os dois achados de forma diferente não se sustenta.
- O ledger copia o YAML da seção 12 sem edição. Aplicar antes de T-0002 e T-0003 congela um contrato que ainda muda, e obriga um segundo diff de ledger para corrigir o primeiro.
- O que não muda: a decisão do editor, os números do S-1 e o aceite dos quatro ADRs. O custo de esperar é uma rodada de execução.

## Registrar

- Item 14 de `adrs/_work/ADR-005-pendencias-ledger.md`: aplicado ao ledger, dentro de `riscos_abertos` do ADR 002.
- No corpo da tarefa, na seção "Resultado": a saída de conferência do passo 5 e o hash dos commits do passo 6.
