---
id: Q-0001
tarefa: T-0001
criada_por: B
criada_em: 2026-09-19T20:10
tipo: parada
bloqueante: true
categoria_aprovacao: nenhuma
---

## Pergunta
Parada 4 do S-1: a correção da fusão de listas do MDXEditor fechou a trava 1 e não fechou a trava 2. Que caminho tomar antes de escrever o ADR 005?

## Contexto

Decisão da parada 4 (`adrs/_work/spike-s1/RESULTADO-S1.md`, seção "Decisão da parada 4"): corrigir a fixture 05 no MDXEditor com três travas, prazo fixo de 1,5 dia, sem prorrogação.

**Trava 1 (critério numérico), atingida.** Saída real em `adrs/_work/spike-s1/mdxeditor/playwright-output-correcao-05.txt`, 135 testes, 127 passam, 8 falham.

| Item | Antes da correção | Depois |
| --- | --- | --- |
| 1A expected | 24/25 | 25/25 |
| 1B input canonical | 16/17 | 17/17 |
| 1C erro | 5/5 | 5/5 |
| E-01 (A + C) | 29/30 | 30/30 |
| Alternância (D-5) | 24/25 | 25/25 |

Nenhum teste que passava antes passa a falhar. D-2 segue 6/8 (26 e 27), fora do prazo por decisão já tomada na própria parada 4.

**Trava 2 (correção de modelo), não atingida no resultado, atingida na forma.** A correção substitui o `$transform` estático do `ListNode` do `@lexical/list` 0.48 (`LexicalList.dev.mjs:1080-1085`), que fundia a lista seguinte do mesmo tipo já na carga. A troca vale para toda instância da página, inclusive editores aninhados. Código em `src/editors/mdxeditor/listNoMerge.ts` (37 linhas) e duas linhas em `dokPlugin.tsx`. Revisão de conformidade feita: nenhum caso especial por fixture, nenhuma chamada a `parseDok`/`serializeDok`/`normalizeDok` dentro do adaptador, `extra/`, `tests/mdxeditor/t14-extra-listas.spec.ts`, `src/shared`, `content-format`, `src/routes`, configs e `package.json` sem alteração (`git diff --stat`).

Casos extras de regressão (`adrs/_work/spike-s1/extra/`, 12 casos, 3 testes cada, exigidos pela trava 2): **30/36**, contra 9/36 na linha de base (`extra/baseline-antes-da-correcao.txt`). Falham os três testes de `x05-ul-ul-varios-blocos` e os três de `x06-ol-ol-varios-blocos`, ambos casos "com item de vários blocos", explicitamente enumerados no texto da trava 2.

Causa das seis falhas: não é fusão, é perda do `spread` da lista frouxa (linha em branco entre itens irmãos). Diff só de linha em branco, nenhum conteúdo perdido:

```
   continuação do um
-
 - dois
```

Origem nos visitors do MDXEditor (`LexicalListVisitor.js`, `LexicalListItemVisitor.js` gravam `spread: false`; `MdastParagraphVisitor.js` usa par de `LineBreakNode`). Correção estimada em 1 a 1,5 dia, com `NodeState` para `spread` e modelo de item com blocos.

**Achado sobre o corpus do ADR 002.** Varredura das 30 fixtures pelo `parseDok` do porte:

| Forma | No corpus | Nos casos `extra/` | MDXEditor |
| --- | --- | --- | --- |
| `listItem` com mais de um bloco | fixture 06 | x05, x06, x09, x11 | 06, x09 e x11 passam |
| `listItem.spread = true` | fixture 06 | x05, x06, x09, x11 | 06, x09 e x11 passam |
| `list.spread = true` (linha em branco entre itens irmãos) | nenhuma | x05, x06, x08, x09 | x08 e x09 passam, x05 e x06 falham |

Nenhuma das 30 fixtures do ADR 002 tem `list.spread = true` na raiz. O E-01 30/30 prova o round-trip do corpus, não prova que uma lista com linha em branco entre itens sobrevive no MDXEditor. Registrado como pendência 14 em `adrs/_work/ADR-005-pendencias-ledger.md`, dono ADR 002 (fatia F5).

**Verificações de bundle**, feitas com `mdxeditor/dist-check` (parada 3, item 2, e D-3): nenhum chunk contém `js-yaml`, `argparse`, `ArgumentParser` ou `YAMLException`; o runtime do Lexical (`createEditor`, `registerNodeTransform`) aparece só no chunk lazy da rota de edição, não nos chunks de leitura (244 e 503 bytes).

Evidência completa: `adrs/_work/spike-s1/RESULTADO-S1.md` (seção "Desfecho da rodada de correção"), `adrs/_work/spike-s1/mdxeditor/RESULTADO.md` (seção "Correção da fixture 05"), `adrs/_work/spike-s1/mdxeditor/playwright-output-correcao-05.txt`, `adrs/_work/ADR-005-pendencias-ledger.md` (item 14).

## Opções

1. **Aceitar o MDXEditor, com a lista frouxa virando fatia obrigatória.** Fecha o S-1 com o MDXEditor. A lista frouxa (1 a 1,5 dia) entra como fatia obrigatória antes de liberar o editor, junto com a exceção ao colar nó `html` (0,5 dia, já prevista na parada 4). D-2 26/27 e notas de rodapé editáveis seguem como custo (C) na ponderação, como já decidido. Custo aceito: a letra da trava 2 pedia os 12 casos passando nos três momentos antes de decidir, e essa opção decide sem esperar isso, tratando a lacuna como fatia planejada em vez de bloqueio. É override explícito da regra escrita da própria parada 4.
2. **Estender a correção mais 1 a 1,5 dia para fechar 36/36 antes de decidir.** Preserva a letra da trava 2. Custo aceito: contraria "prazo fixo, sem prorrogação" da parada 4, e adia a decisão do S-1.
3. **Aplicar a regra do Milkdown do ADR 002** (nenhum candidato passou nos testes 1 e 3 ao pé da letra, repetir com Milkdown). Custo aceito: descarta um E-01 30/30 com defeito localizado, orçado e sem perda de conteúdo, por causa de uma forma (lista frouxa na raiz) que o próprio corpus do ADR 002 não testa. Milkdown já ficou de reserva na D-5 e arrasta Vue, CodeMirror e KaTeX, segundo a ficha da candidata.

Descartada sem numerar: normalizar `spread` no `normalizeDok` para o editor nunca ver lista frouxa. Dissolveria o defeito redefinindo o formato, mudaria o contrato do ADR 002 fora desta sessão e apagaria intenção autoral que muda o HTML renderizado.

## Recomendação de B
Opção 1. O E-01 fecha em 30/30 com correção de modelo revisada, sem regressão, e a lacuna restante (lista frouxa na raiz) tem causa, estimativa e caminho de correção claros, além de não ser exercitada pelo corpus do ADR 002 que a própria regra de aceite usa como critério. Tratá-la como fatia obrigatória, junto com a exceção ao colar `html`, é mais barato e mais honesto que reabrir com o Milkdown ou prorrogar um prazo que a parada 4 fixou como não prorrogável.
